<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Card - Tailwind</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="flex flex-wrap gap-8 items-center justify-center h-screen bg-gray-100">

    <% allUsers.forEach(val=> { %>
        <% if(val.length===0) {%>
            <h3 class="text-zinc-600 text-2xl ">No Users Found!</h3>
            <% } else{ %>
                <div class="bg-white rounded-xl shadow-lg p-6 w-72 text-center">
                    <img class="w-24 h-24 mx-auto rounded-full object-cover mb-4" src='<%= val.image %>'
                        alt="User Avatar">
                    <h2 class="text-xl font-semibold mb-1">
                        <%= val.username %>
                    </h2>
                    <p class="text-gray-400 text-sm mb-4">
                        <%= val.email %>
                    </p>
                    <div class="flex items-center justify-center gap-4">
                        <a href="/delete/<%=val.id_%>" class="text-center text-blue-500 font-bold">Delete</a>
                        <a href="/edit/<%=val.id_%>" class="text-center text-blue-500 font-bold">Edit Profile</a>
                    </div>
                </div>
                <%}%>
                    <%})%>

</body>

</html>